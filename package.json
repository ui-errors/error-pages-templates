{
  "name": "error-pages-templates",
  "version": "2.0.0",
  "private": true,
  "description": "UI-Errors template registry system",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rimraf build registry/.build-cache.json",
    "rebuild": "npm run clean && npm run build"
  },
  "dependencies": {
    "cheerio": "^1.1.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
